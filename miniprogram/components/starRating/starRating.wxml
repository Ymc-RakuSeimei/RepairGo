<view class="star-rating">
  <view
    class="star {{item <= rating ? 'active' : ''}}"
    wx:for="{{stars}}"
    wx:key="*this"
    data-value="{{item}}"
    bindtap="onTapStar"
    style="font-size: {{size}}rpx"
  >
    {{item <= rating ? '★' : '☆'}}
  </view>
</view>
